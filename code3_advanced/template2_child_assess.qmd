
## What kinds of responces did users submit?

```{r}
# https://towardsdatascience.com/create-a-word-cloud-with-r-bde3e7422e8a

# Install libraries ------------------------------------------------------------
PKG <- c(
  "wordcloud", 
  "wordcloud2"
)

for (p in PKG) {
  if(!require(p,character.only = TRUE)) {  
    install.packages(p, verbose = FALSE)
    require(p,character.only = TRUE)}
}
```


```{r}

dat1 <- data.frame(
  table( # find frequency of terms used
    data.frame(t( # slightly transform input data
      dat0[,-c(1:2)])))) # remove Timestamp and Author columns

wordcloud::wordcloud(words = as.character(dat1$X1), 
          freq = as.numeric(dat1$Freq), 
          min.freq = 1,
          scale = c(1,3),
          max.words=200,
          random.order=FALSE, 
          rot.per=0.35
          )

```
